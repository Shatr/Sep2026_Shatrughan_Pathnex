what i learn today!


So in today video we create and manage the infrastructure with teraform and created an EKS cluster in it
Also seen till now whatever resource created in k8s with the help of kubernetes yaml file that is useful
still in organization it is very hard to manage multiple yaml file so instead of that we written a helm chart by bitnami repo
in which we have seen the core of helm is deployment, services, chart & value.yml & imp file _helper.tpl which is a reusable function
file which is used to create the helm chart and call the api of the helm, just need to change the value in value.yml and sync with chart.yml
need to update the version accodingly depend upon the change. 

there is major,minor,bugfix release is there that accordingly need to change in it and update it into it

SO we have seen we add the helm repo then install the repo with release name & repo name & upgrade the helm chart and push it to the repository 
which is easily manageable again & again we dont have to write the multilple yaml file in k8s resource,so we leant helm based deployment as well

Also seen august batch video.
